{
	"name": "ImmuDB PoC",
	"dockerComposeFile": "docker-compose.yml",
	"service": "development",
    "workspaceFolder": "/vscode",
	"remoteEnv": { "HPWD": "${localWorkspaceFolder}" }
	

	// Add the IDs of extensions you want installed when the container is created.
	"extensions": [
	],

	"remoteUser": "vscode"
}